# Site design constraint

The user approved the current notebook design and explicitly asked to lock the CSS.
Preserve `assets/site.css` unchanged unless the user explicitly authorizes a style
change. Use existing classes for new pages and content. Do not work around this
constraint with inline styles, additional stylesheets, or JavaScript styling.

Keep the site compatible with GitHub Pages. Do not add family information.
Get user approval before pushing new work; an explicit request to push is approval.
